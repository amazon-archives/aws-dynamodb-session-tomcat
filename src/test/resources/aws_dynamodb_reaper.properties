# Set this attribute to false if you are configuring TTL to run and
# only want TTL to remove sessions.  Set to true if you don't care that
# the reaper and TTL could possibly removing sessions.
aws.dynamodb.run.reaper=true